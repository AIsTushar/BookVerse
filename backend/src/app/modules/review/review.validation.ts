
import { z } from "zod";

const createReviewSchema = z.object({
	
});
const updateReviewSchema = z.object({
	
});

export const ReviewValidations = {
	createReviewSchema,
    updateReviewSchema,
};

