
import { z } from "zod";

const createProductSchema = z.object({
	
});
const updateProductSchema = z.object({
	
});

export const ProductValidations = {
	createProductSchema,
    updateProductSchema,
};

